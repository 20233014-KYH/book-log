# 프로젝트 진행 규칙

## 응답 언어
- 항상 한국어로 답한다.

## 기술 스택
- Next.js (App Router)
- TypeScript
- Tailwind CSS
- Supabase
- 배포: Vercel

## 작업 방식
- 코드를 수정한 뒤에는 무엇을, 왜 바꿨는지 초보자가 이해할 수 있도록 3줄 이내로 설명한다.
- 한 번에 한 가지 기능만 작업한다. 여러 기능을 한꺼번에 건드리지 않는다.
- 새로운 라이브러리를 추가하기 전에는 반드시 먼저 물어보고 사용자의 승인을 받는다.

## 보안
- 비밀 키(API 키, 토큰 등)는 항상 `.env.local`에 넣는다.
- 비밀 키를 코드에 직접 작성하지 않는다.
